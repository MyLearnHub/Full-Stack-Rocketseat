# Operadores Lógicos

---

```js
let email = true;
let password = false;
let isAdmin = true

// AND (E) &&
console.log(email && password && isAdmin);

// OR (OU) ||
console.log(email || password);

// NOT (negação) !
console.log(!password);
```
